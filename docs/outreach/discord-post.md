# Discord Post

```text
Hi Tenstorrent community, RQM Technologies is building an independent open-source structured-kernel benchmark called tt-rqm-kernels / StructuredBench.

It uses ordinary floating-point tensors to represent quaternion and rotor values, starting with [N, 4] = [real, i, j, k]. The first target is qmul, followed by streamed qrotate_vector.

For a first external structured-kernel contribution, should we target a TT-Metalium programming example or a TT-NN custom-op path?

Repo: https://github.com/RQM-Technologies-dev/tt-rqm-kernels
```
